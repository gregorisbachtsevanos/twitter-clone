<nav
    class="navbar navbar-expand-lg flex-column h-100 position-fixed navbar-light bg-light"
    style="width: 10%"
>
    <div class="container-fluid d-block flex-column w-100">
        <button
            class="navbar-toggler m-auto"
            type="button"
            data-bs-toggle="collapse"
            data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent"
            aria-expanded="false"
            aria-label="Toggle navigation"
        >
            <span class="navbar-toggler-icon m-auto"></span>
        </button>
        <div
            class="collapse navbar-collapse flex-column"
            id="navbarSupportedContent"
        >
            <ul class="navbar-nav me-auto mb-2 mb-lg-0 flex-column m-auto">
                <li class="nav-item">
                    <a class="nav-link" aria-current="page" href="<%= currentUser.username %> ">
                        <% if (currentUser.extra_info.avatar){ %> 
                        <img src="/images/avatarImage.png" alt="avatar" height="100%" width="100%"></a>
                        <% } else { %>
                            <p class="h1 text-dark"><%= currentUser.username.charAt(0).toUpperCase() %></p> 
                        <% } %> 
                </li>
                <li class="nav-item">
                    <a class="nav-link" aria-current="page" href="<%= appUrl %>">Homepage</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" aria-current="page" href="#">Chat</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" aria-current="page" href="#">Notification</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" aria-current="page" href="#">Trending</a>
                </li>
                <li class="nav-item">
                    <form action="/logout?_method=DELETE" method="post">
                        <button class="nav-link btn" aria-current="page" href="/logout">Logout</button>
                    </form>
                </li>
            </ul>
        </div>
    </div>
</nav>
